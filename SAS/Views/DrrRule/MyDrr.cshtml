@model SAS.Models.DrrRule

@{
    //所有酒店列表

    List<SAS.Models.hotel_info> hotels = ViewData["hotels"] as List<SAS.Models.hotel_info>;

    //所有酒店对应的房型列表
    List<SAS.Models.hotel_room_info> rooms = ViewData["rooms"] as List<SAS.Models.hotel_room_info>;
    //促销信息列表
    List<SAS.Models.DrrRule> Drrs = ViewData["drrs"] as List<SAS.Models.DrrRule>;

    ViewBag.Title = "MyDrr";
}

<div id="DrrRule" class="box_a">
    <h2>公寓:
        <select name="" >
            @foreach (var h in hotels)
            {
                <option value="@h.">@h.h_name_cn</option>
            }
        </select>
    </h2>
    <table cellspacing="0">
        <tbody>
            <tr>
                <th>担保规则</th>
                <th>促销日期</th>
                <th>髟房型</th>
                <th>操作</th>
            </tr>
            <tr>
                <td>td</td>
                <td>td</td>
                <td>td</td>
                <td>td</td>
            </tr>

        </tbody>
            @foreach (var h in hotels)
            {
                var Rooms = rooms.Where(r=>r.hotel_id== h.hotel_id);
                //当前酒店对应的房型
                foreach (var item in Rooms)
                {
                    @item.h_r_name_cn
                }
                
            }

    
</div>

