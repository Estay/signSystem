@model SAS.Models.News

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>News</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Ttitle)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Ttitle)
            @Html.ValidationMessageFor(model => model.Ttitle)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Descripiton)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Descripiton)
            @Html.ValidationMessageFor(model => model.Descripiton)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
